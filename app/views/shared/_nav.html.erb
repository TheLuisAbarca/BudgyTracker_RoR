<nav class="navbar navbar-expand-lg navbar-dark bg-blue">
  <div class="container-fluid">
    <% if ( name === 'TRANSACTIONS' || name === 'NEW TRANSACTION' || name === 'NEW CATEGORY' || name === '')%>
      <span>
        <%= link_to path, class: "bg-transparent text-light border-0", :method => :get do%>
          <span class="material-icons nav-icon-white ">
          west
          </span>
        <%end%>
      </span>
    <% else %>
      <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
      </button>
    <% end %>
    <h3 class="d-flex align-items-center text-white text-center ms-auto">
      <%= name %>
    </h3>
    <span class="material-icons navbar-brand ms-auto nav-icon-white">
      search
    </span>
    <div class="collapse navbar-collapse" id="navbarNav">
      <ul class="navbar-nav me-auto">
        <li class="nav-item">
          <%= button_to 'Log Out', destroy_user_session_path, method: :delete, class: "bg-transparent border-0 text-light"%>
        </li>
      </ul>
    </div>
  </div>
</nav> 